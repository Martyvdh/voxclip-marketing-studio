# CLAUDE.md

Read `AGENTS.md`. It is the binding rule set for this repository and applies to Claude Code,
Cowork, and every dispatched agent.

Before writing any user-facing words, read `docs/brand.md` and `docs/product-truth.md`.

Before claiming any work is finished, run `npm run verify` and cite the fresh output.
